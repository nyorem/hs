Some things to know about Parsec:

- Parser = GenParser Char ()
- Functions:
-   --> many p = tries to parse p zero or more and gathers the result in a list
-   --> char c = parses the character 'c'
-   --> string str = parses the string 'str'
-   --> noneOf str = tries to parse a character not included in 'str'
-   --> eof = end of file marker
-   --> sepBy f g = f is separated by g
-   --> endBy f g = f terminates with g

